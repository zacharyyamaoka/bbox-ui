{
  "name": "@bbox-ui/compare-view",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The reusable host-comparison view: Split / fullscreen / Overlay modes, linked cameras across the tldraw and React Flow camera models, and the numeric divergence readout — over any Scene. demos/compare feeds it the fixed demo scene; the playground feeds it a scene derived from its live board.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@bbox-ui/adapter-reactflow": "workspace:*",
    "@bbox-ui/adapter-tldraw": "workspace:*",
    "@bbox-ui/core": "workspace:*",
    "@bbox-ui/demo-scene": "workspace:*",
    "@xyflow/react": "^12.8.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tldraw": "5.3.2"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
