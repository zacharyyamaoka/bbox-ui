{
  "name": "@bbox-ui/adapter-tldraw",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "tldraw host adapter: a ShapeUtil rendering the bbox-ui core inside HTMLContainer; port anchors are geometry points, not DOM elements.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@bbox-ui/core": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "tldraw": "5.3.2"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "react": "^19.0.0",
    "tldraw": "5.3.2",
    "typescript": "^5.6.0",
    "@tldraw/tlschema": "5.3.2"
  }
}
