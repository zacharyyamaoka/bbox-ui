---
title: Components
description: Presentational components for black-box system design.
---

Every component here is copy-paste: you own the source, there is no runtime
dependency on this project. The core imports no canvas engine, so the same
component paints identically inside React Flow, inside tldraw, or in a plain
page.

<ComponentsList />
